name: md-to-pdf

on: push

jobs:
  convert_via_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - run: ls
      - uses: docker://pandoc/core:2.9
        with:
          args: >-
            --output=README.pdf
            ./README.md

